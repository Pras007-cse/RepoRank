# web/ — not yet built

This is where the Phase 5 frontend goes (discovery feed, project submission
form, project pages, connection UI) — see the top-level README's "Status"
section for what the `worker/` API already supports.
